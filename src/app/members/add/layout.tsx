import CenterLayout from "@/components/common/CenterLayout";

const AddMemberLayout = ({ children }: { children: React.ReactNode }) => {
  return <CenterLayout>{children}</CenterLayout>;
};

export default AddMemberLayout;
